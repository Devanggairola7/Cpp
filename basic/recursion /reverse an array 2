#include<iostream>
#include<bits/stdc++.h>
void f(int i,int arr[],int n){
    if(i>=n/2){return ;}
    std::swap(arr[i],arr[n-i-1]);
    f(i+1,arr,n);
}
int main(){
    int arr[5]={1,2,3,4,5};
    f(0,arr,5);
    for(int i=0;i<5;i++){
        std::cout<<arr[i]<<std::endl;
    }
}
